package com.inv.walletCare.logic.entity.account;

/**
 * Defines the types of accounts available in the system.
 * Each account type is represented by a unique string value.
 */
public enum AccountTypeEnum {
    /**
     * Represents a personal account type.
     */
    PERSONAL,
    /**
     * Represents a shared account type.
     */
    SHARED
}